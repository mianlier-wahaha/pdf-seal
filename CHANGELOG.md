# 更新日志 / Changelog

所有重要改动记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.0.1] - 2026-09-05

### 修复
- **另存为文件名后缀在中文界面下出错**：原实现把本地化键「文件名后缀」直接当作后缀写入文件名，简体/繁体界面下会生成「合同-文件名后缀.pdf」这类把说明文字当后缀的错误文件名；英文后缀 `seam-stamped`（骑缝）也与「同时支持正文章」的事实不符
- 改为按界面语言取专用后缀：简体「已盖章」、繁体「已蓋章」、英文 `stamped`；另存为默认文件名形如「检测报告-已盖章.pdf」「report-stamped.pdf」，保存位置仍默认为原文件所在文件夹
- **选中章后其他章点不中**：四角控制点原本渲染在选中章之后，在 ZStack 中夹在「选中章」与「更晚添加的章」之间，Z 序更低的章被它遮挡，只有更晚添加的章能点中；同时控制点用 `.position()` 定位，会把视图的布局 frame 撑满整个父容器，hit area 随之扩大到接近整页，连 hover（小手）与点击一并吞掉。现改为所有章渲染完后再统一绘制控制点（独立置于最上层），定位改用 `.offset()`，hit area 收回控制点自身大小
- **⌘ 点击多选此前不生效（检测方式错误）**：原实现在章的 `.onTapGesture` 闭包里读 `NSEvent.modifierFlags` 判定 ⌘，而该闭包是手势结束后**异步派发**的，此时全局修饰键状态常已重置，判定几乎恒为 false，导致 ⌘ 点击只是普通单选。现改为由 `ContentView` 的 `.flagsChanged` 全局监听器实时把 ⌘ 状态写入 `settings.commandKeyDown`，点击时直接读该实时值，判定可靠

### 改进
- **正文章选中态终于可见**：此前点击空白处新增章后会立刻把选中状态清空，而章本身又没有点击手势，导致选中框永远不会出现。现在新增章后自动进入选中态并显示控制框与右栏「章体大小」滑杆；再新增下一枚时选中自动转移到新章、上一枚自动取消；按 Esc 取消选中
- **选中视觉显著增强**：由 1.5px 橙色细虚线改为「白色衬底 + 系统强调色虚线」双层描边并叠加淡底色，在白纸 / 红色章面 / 深色背景上均清晰可辨
- **点击章本身即可选中**（拖动优先，纯点击才触发）；四角控制点统一为强调色并加大至 12–14px，更易抓取
- **右栏新增「已选中第 N 枚 / 共 M 枚」信息行 + 移除按钮**：选中预览中的章后，在「章体大小」上方显示当前是第几枚（共几枚），并提供移除按钮，删除该章后自动切到下一枚（保持选中），无章可切则自动取消选中
- **键盘 Delete 键删除选中章**：与右栏移除按钮同效；仅拦截裸按的 Delete（kVK_Delete = 51），带 ⌘/⌥ 修饰的组合键放行给系统，避免与「删除到行首」等编辑快捷键冲突；文本编辑器激活时（页码输入框等）完全不拦截
- **正文章支持多选（⌘ 点击累加）**：已有一枚或多枚章被选中时，按住 ⌘ 点击任意章会在选中集合里累加该章、其余已选中章的选中状态保持不变；⌘ 点击已选中的章则取消其选中（标准切换逻辑）。普通点击仍只选中被点章（清空其余）；按 Esc 取消全部选中。选中框为每枚选中章都显示，缩放手柄仅显示在主选中（最后点击/添加的章）上，与访达/预览一致
- **右栏「移除」按钮与 Delete 键支持多选**：选中多枚时「移除」按钮与 Delete 键一并删除所有选中章；「已选中第 N 枚 / 共 M 枚」改为「已选中 N 枚 / 共 M 枚」计数；尺寸滑杆仅在单选时显示（调整主选中章），多选时隐藏以避免歧义

## [2.0] - 2026-09-05

### 新增
- **新建图章对话框实时尺寸**：导入图片后，尺寸(cm) 输入框自动按图片真实物理尺寸填充（读取 DPI 元数据换算；无 DPI 信息按 300 DPI 估算），不再固定 4×4
- **选中章尺寸滑杆**：点击已添加的正文章选中后，右栏出现「章体大小」滑杆，实时等比调整选中章大小（1–20cm），上方实时显示「宽 × 高 cm」数值；松手固化到该章默认尺寸，⌘Z 可撤销
- **骑缝章多条错开**：支持同一 PDF 添加多条骑缝章实例，新增「上 ↔ 下」偏移滑杆（拖动实时移动最新一条），同页多章可上下错开互不覆盖
- **关闭确认**：点击「关闭」弹出「是否确认关闭？」确认框（取消/确定）
- **预览状态栏**：底部新增缩放百分比输入框（内置下拉预设 25/50/75/100/150/200%）与页码跳转输入框（显示「/ 总页数」）
- **双指捏合缩放**：触控板双指捏合实时缩放预览（10%–400%），与状态栏联动
- **保存/另存为/关闭按钮**：保存直接覆盖当前文件；另存为默认定位原文件夹、文件名默认「原文件名-骑缝章.pdf」

### 改进
- **章实例锁定印章**：每枚章（含骑缝章）添加时锁定所用印章图，切换印章库不影响已盖的章；支持一份 PDF 多章混盖（导出器升级为按放置项携带章图）
- **物理尺寸模型重构**：章实例直接存储宽/高物理厘米值，同一枚章在 A4/Letter 等不同纸张规格上保持相同实际大小；修复「新建图章设置宽高不生效」
- **深色模式**：全面适配系统深色外观（透明化预览棋盘格自动调暗）
- **三语言本地化**：界面跟随系统语言，支持 English / 简体中文 / 繁體中文
- **多语言 README**：新增英文版与繁体中文版，主 README 提供语言切换链接
- **代码清理**：移除无引用代码；保存/导出结果反馈显示于状态栏左侧
- **界面精简**：移除冗余滑杆与开关（启用开关、章体大小/位置偏移/不透明度/旋转滑杆），参数调整改由预览交互（点击落章、拖点缩放、滑杆）完成

### 修复
- 修复「全部页」只盖最后一页的问题（预览与导出统一范围逻辑）
- 修复 Esc 无法取消选中章、⌘Z 撤销失效（字段编辑器占据响应链导致按键拦截失效）
- 修复骑缝偏移滑杆标注上下颠倒、无法铺满行宽（自绘滑杆实现）
- 修复控制点与虚线框错位（热区参与布局导致偏移）

## [1.0] - 2026-09-04

### 首个公开版本
- SwiftUI + PDFKit/CoreGraphics 原生实现，零第三方依赖，纯本地处理
- 骑缝章：右/左/上/下缝位、页范围、按页删除与恢复
- 正文章：点击落章、拖动移动、右下角拖点缩放、右键按页/整章删除、多章实例
- 印章库：图片导入、白色转透明（容错可调、实时棋盘格预览）、长宽比锁定、物理尺寸设定（cm）
- 拼合校验视图：各页章条按页序拼回完整章对照检查
- 键盘操作：Esc 取消选中、⌘Z / Ctrl+Z 逐步撤销（每章独立撤销栈）
- 无头验证工具 SealTool（旋转页/混合尺寸页盖章回归测试）
