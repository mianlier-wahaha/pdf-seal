# 更新日志 / Changelog

所有重要改动记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.2.16] - 2026-09-12
### 新增
- **访达「打开方式」支持**：在访达中右键 PDF →「打开方式」可直接选择「PDF骑缝章」；双击 PDF 也能在系统的「打开方式」里选本 app 打开并自动载入该文件。

### 技术
- `Info.plist` 新增 `CFBundleDocumentTypes`（`com.adobe.pdf`，Editor 角色，Alternate 优先级），打包后用 `lsregister -f` 强制注册 LaunchServices。
- `AppDelegate` 在 `applicationWillFinishLaunching` 注册 `kAEOpenDocuments` Apple 事件处理器，提取文件 URL 载入；冷启动时事件先于 `bind` 到达的场景用 `pendingOpenURL` 暂存补载。

## [2.2.15] - 2026-09-12
### 新增
- **窗口关闭未保存确认**：点击窗口红 X（或工具栏「关闭」）时，若当前文档有未保存改动（已添加/调整印章或水印），弹出「该文件尚未保存，是否关闭？」（按钮：取消 / 确认）；未修改或已保存/已导出状态下点击关闭则直接关闭，不再询问。

### 技术
- `DocumentStore` 新增 `isDirty` 脏标记；`AppDelegate` 监听 `qifengStamps`/`fullStamps`/`watermark` 三个 `@Published` 变更自动置脏（仅内容变更，选中章等状态变化不触发）。
- 通过 `NSWindowDelegate` 转发代理（`CloseGuard`）拦截 `windowShouldClose`，保留 SwiftUI 原生 window delegate 行为；保存（原地覆盖）与另存为成功后置 `isDirty = false`。

### 修复
- **修复启动即卡死（99% CPU 死循环）**：`bind()` 原本写在 `App.body` 内，每次 SwiftUI 重算 body 都会新建 3 个 `sink`；而 Combine 订阅 `@Published` 会立即重放当前值，把 `isDirty` 误置 `true` → 视图失效 → 再重算 body → 再建 sink…，形成无限重绘且订阅集合无限膨胀。改为 `bind` 加 `didBind` 幂等守卫（仅首次真正订阅），并用 `dropFirst()` 跳过订阅瞬间的初始重放，避免启动即误报未保存。

## [2.2.14] - 2026-09-12
### 回退
- **整体回退 2.2.13 的纸厚渐变功能**：该改动导致骑缝章预览/导出异常（功能失效）。删除 `StampPlacement` 的 `fadeWidth`/`fadeAlpha`/`fadeAtMaxX`、`QifengConfig` 的 `fadeWidthMm`/`fadeEdgeAlpha`、导出端灰度 mask 与预览端 `LinearGradient` mask，行为恢复至 2.2.12。预览端保留 `.opacity(pl.opacity)`（修复预览忽略透明度的既有不一致，风险极低）。

## [2.2.13] - 2026-09-12
### 新增（⚠️ 已在 2.2.14 整体回退）
- **纸厚渐变（骑缝章边缘由浅入深）**：内页→尾页的骑缝章条朝页心一侧边缘不再是一条生硬直线，而是模拟纸张厚度带来的颜色过渡。默认 `fadeWidthMm = 0.05mm`、`fadeEdgeAlpha = 0.9`，边缘深度为章的 90%，向内线性渐变到 100%。右缝渐变位于条左缘，左缝渐变位于条右缘；首页不带渐变。
- `StampPlacement` 新增 `fadeWidth`/`fadeAlpha`/`fadeAtMaxX` 三个字段。
- `QifengConfig` 新增 `fadeWidthMm`/`fadeEdgeAlpha`（默认 0.05mm / 0.9）。
- 导出：`PDFExporter.drawPlacement` 在渐变带外全深度绘制，渐变带内使用灰度 mask 实现 90%→100% 线性过渡。
- 预览：骑缝章切片补上 `.opacity(pl.opacity)`，预览与导出所见即所得；渐变带用 `LinearGradient` mask 同步。

### 内部
- SealTool 场景 6 像素级验证：纯色方章 + 10mm 放大渐变带 + 4x bitmap 直接采样，确认边缘 g≈0.075、中点 g≈0.039、内侧 g≈0.000。

## [2.2.12] - 2026-09-12
### 变更
- **默认占比调整**：首页占比默认 25%（不变）；**内页占比默认由 15% 改为 5%**——中间页更纤细，单枚印章可覆盖更多页数，减少平铺场景。

## [2.2.11] - 2026-09-12
### 变更
- **骑缝章仅保留左/右缝**：移除上缝、下缝选项及对应代码；`SeamEdge` 枚举、`StampGeometry.qifeng`、L10n 三语、UI Picker 同步清理。
- **首页占比加大新增「内页占比控制」开关**：
  - **开启**：沿用 v2.2.9/v2.2.10 逻辑——首页固定占比、中间页固定内页占比、尾页取剩余并贴到章最右缘。
  - **关闭**：改为「首尾页各占首页占比，中间页均分剩余占比」模式；首尾页占比一致，中间页自动平均，尾页仍贴到章最右缘。更贴近多页文件常规盖章习惯。
- `QifengConfig` / `QifengInstance` / `StampSettings` 新增 `middleRatioEnabled`；Stores/ContentView/PreviewPagesView/ParamsPanelView 同步传递。

## [2.2.10] - 2026-09-12
### 变更
- **尾页骑缝章位置内移**：尾页章条不再贴页面边缘，向页心方向内移整章目标宽（左右缝）/高（上下缝）的 **1/3**，贴近现实手工盖章时尾页章的落位习惯。其余页仍贴边缘缝。均分模式与「首页占比加大」模式均生效；内移后仍受页面边界 clamp 保护，不会溢出。

## [2.2.9] - 2026-09-12
### 修复
- **尾页骑缝章盖不到印章最右侧（致命）**：v2.2.7 的「尾页取剩余」仅在 `首页占比+Σ内页占比<100%` 时成立；当恰好占满或溢出（如首页 25% + 内页 15%、共 7 页以上）时走平铺分支，尾页也按内页占比取段——源区间落在章中间甚至 wrap 回章左侧，尾页永远贴不到章的右缘。修复：**尾页源区间终点统一对齐到下一个整数边界**（本份印章最右缘）——首页 + 中间页沿缝连续累积后，尾页从累积位置取到 `ceil(累积位置)`，剩多少取多少；累积位置恰好压在整数边界时尾页取完整一份。任何页数、任何占比组合下尾页都精确盖到章最右缘（SealTool 无头验证 2/5/6/7/8/10 页 + 均分模式全部对齐 1.0）。

## [2.2.8] - 2026-09-12
### 修复
- **骑缝章同页内多段重叠**：当某页切片跨印章边界 wrap（多份印章平铺）时，该页目标区域被拆成两段绘制，但两段横向/纵向偏移仍按 `sliceW/sliceH`（已乘 weight）计算，而宽度/高度已改为按整章 `w/h` 计算，导致偏移量小于宽度、两段互相重叠。修复：偏移也按整章 `w/h` 累计（`dx = x0 + seg.destFrac * w`、`dy = y0 + seg.destFrac * h`），使 wrap 后的各段在页边缘精确相接、不再重叠。

## [2.2.7] - 2026-09-12
### 变更
- **「首/尾页占比加大」改为「首页占比加大」**：修复尾页骑缝章无法盖到印章最右侧的 bug。原方案要求「首尾页各占 X%、内页各占 Y%」两个条件同时成立，两者相加往往不等于 100%，尾页被迫取一个凑数比例、盖不到章的右边缘。现改为：**首页占「首页占比」、中间各页占「内页占比」，尾页不再单独设定占比，直接取剩余部分**——剩余为正时尾页贴着印章最右边缘收尾；剩余非正（占比之和已超 100%）时自动退回多份印章平铺逻辑。
- 滑杆范围：首页占比 10%–40%（默认 25%），内页占比 5%–40% 保持不变；说明文案同步更新（简/繁/英）。

## [2.2.6] - 2026-09-12
### 修复
- **骑缝章切片被纵向/横向「拉高」**：v2.2.4/v2.2.5 的加权切分算法中，dest 子段尺寸误多乘了一次 `weight`——代码把每页贴片总宽度 `sliceW = w * weight` 当作了「单份源图宽度」，再乘上源图子段占比 `seg.srcW`，导致每页实际贴片宽度变成 `w * weight * seg.srcW`。`weight` 越小（中间页），切片被压得越窄、视觉上越「瘦高/拉长」。
- 修复：dest 子段宽度/高度统一改为 `seg.srcW * w` / `seg.srcW * h`（即「源图子段占比 × 整章目标尺寸」），与 `sliceW/sliceH` 解耦。这样无论 `weight` 是多少、是否跨印章边界拆片，每页贴片的宽高比都与裁剪出的源图一致，不再变形。预览与导出共用同一算法，同时生效。

## [2.2.5] - 2026-09-12
### 新增
- **骑缝章「内页占比」独立设置**：在「首/尾页占比加大」开关下新增「内页占比」滑杆（5%–40%，默认 15%）。开启后，首尾页各占「首/尾页占比」，其余每页各占「内页占比」，不再「均分剩余」。
- **多份印章平铺**：当首尾页与内页各页占比之和超过 100%（即超过一枚印章能覆盖的范围）时，自动以「多份印章」形式呈现——源图在缝方向 wrap 平铺成多份，各页取对应段；跨整数边界的条自动拆成两片拼接，且每页贴片始终位于边缘缝、不溢出页面。拼合校验区会显示多份完整印章拼接的效果。
- 实现位于 SealCore `StampGeometry.qifeng`：按缝方向为各 slice 连续累积源图起始位置，并用 `sourceSegments` 把区间映射到单张章图 `[0,1)`（wrap + 拆片）；dest 始终贴边缘缝，宽度按权重分配。

## [2.2.4] - 2026-09-12
### 新增
- **骑缝章首/尾页占比加大**：骑缝章功能区新增开关「首/尾页占比加大」。开启后，首/尾页各占设定的比例（默认 25%，可在 10%–40% 间调整），除去首/尾两页后剩余页面均分剩余占比；关闭时恢复原有均分逻辑。
- 实现位于 SealCore `StampGeometry.qifeng`：根据页数计算加权切片权重，源图切分与目标页贴片尺寸均按同一套权重分布；页数不足 3 页时自动退化为均分，避免边界异常。
- UI 位于 ParamsPanelView 骑缝章分区：开关下方展开占比滑杆，实时显示百分比，并附带说明「开启后首尾页各占上述比例，其余页均分剩余占比」。调整滑杆会同步更新最后一条骑缝章实例，并作为下一条的默认值。

## [2.2.3] - 2026-09-12
### 修复
- **盖章后光标/焦点仍留在右栏页码框，Delete 删不掉刚盖的章**：点「添加」后 `addFullStamp` 虽已选中刚盖的章，但页码 `TextField` 仍是 first responder（field editor），按下 Delete 被全局键盘监听的文本输入守卫拦截、去删页码数字。
- 修复：在「正文章」和「骑缝章」两个「添加」按钮动作末尾调用 `NSApp.keyWindow?.makeFirstResponder(nil)`，把焦点从右栏文本框移走，使刚盖的章成为 Delete/撤销/方向键等全局快捷键的事实目标。

## [2.2.2] - 2026-09-12
### 修复
- **印章库名字单击即改导致 Delete 误删名字**：原 `SealRow` 的章名是个常驻 `TextField`，单击即获得焦点（field editor），于是「在 PDF 添加章后按 Delete」被全局键盘监听的文本输入守卫拦下、去删库里选中章的名字，而不是删 PDF 上的章（与用户预期相反）。
- 改为：章名默认是普通 `Text`，单击只选中印章、**不会进入编辑**；改名须**右键印章**弹出菜单选「重命名」才出现可编辑框并自动聚焦，回车提交、Esc 取消。右键菜单另含「删除」。这样日常浏览/选中时 field editor 不会被名字框占用，Delete 稳定作用于 PDF 上的选中章

## [2.2.1] - 2026-09-12
### 修复
- **全局快捷键整批失效**：Delete / ⌘Z / Ctrl+Z / Esc / ↑↓ 一组快捷键在 v2.2.0 起不可用。根因是这些快捷键原由 `ContentView.onAppear` 安装、`onDisappear` 卸载的 NSEvent 本地监听器处理；该监听器随视图生命周期装/卸，在窗口或视图身份变化、`onDisappear` 触发后未重装的场景下会整批静默失效（v2.2.0 引入更多常驻文本框后更易触发）。
- 修复：将全局键盘监听器迁移到 App 生命周期——新增 `AppDelegate`（`NSApplicationDelegateAdaptor`），在 App 启动、store 绑定后**安装一次**、`applicationWillTerminate` 时卸载，彻底摆脱视图生命周期的干扰；`ContentView` 中对应的 `@State` 监听器与 `onAppear/onDisappear` 装/卸代码已移除。文本字段编辑时的「输入字符放行系统」守卫保留，⌘ 点击实时状态（flagsChanged）改由 AppDelegate 维护

## [2.2.0] - 2026-09-10
### 新增
- **文本水印**：右栏新增「水印」分区，启用后为整份 PDF 添加文本水印
  - 内容：文本、字体（宋体/黑体/楷体/仿宋）、字号（8–96pt）、颜色（灰/黑/红/蓝）
  - 外观：旋转（0°/30°/45°/60°/90°）、透明度滑杆（1–50%）
  - 多行水印：**单行 / 一页两行 / 平铺**（平铺自适应间距铺满整页，上限 80 枚防极端性能）
  - 位置：水平/垂直对齐（居左中右 × 居上中下）+ 毫米级偏移滑杆
  - 应用于：全部页或指定起止页（与骑缝章页码范围控件同风格）
- **所见即所得**：预览页水印与导出共用同一绘制引擎（SealCore/WatermarkRenderer，CoreText），调参实时刷新预览
- **纯水印导出**：不盖章、只加水印也可「保存/另存为」（原有「请先添加印章」限制放宽）
- 水印绘制在原页之上、章的下层，与章的选中/拖拽/撤销体系完全独立；水印垫底顺序与 WPS 等主流实现一致

## [2.1.1] - 2026-09-05
### 修复
- **光标样式残留**：调完印章库宽度后，光标在 PDF 页面以外的所有区域（参数面板、预览区空白处等）一直显示左右拖移箭头。根因是宽度拖动触发视图重建、`onHover` 重复执行，`NSCursor.push()/pop()` 配对失衡导致光标栈残留 `resizeLeftRight`；现改为进入边界时 `NSCursor.resizeLeftRight.set()` 直接替换、离开时 `NSCursor.arrow.set()` 显式恢复箭头，`set()` 无配对问题，任何情况下不再残留

## [2.1.0] - 2026-09-05
- **印章库支持重排**：选中印章后按 **↑ / ↓** 上下移动其顺序；也可在列表里**直接拖拽**行进行排序（拖起时章图跟随光标，实时换位），重排结果会持久化到 manifest.json（按添加时间顺位）
- **印章库宽度可调**：鼠标移到印章库右侧边界（分隔条）时高亮并显示左右箭头光标，拖动即可调整宽度（150–420pt），宽度跨启动记忆
- **拖拽/键位与已有交互不冲突**：↑/↓ 仅在非文本编辑时生效（与现有 Delete / Esc / ⌘Z 同范围），编辑印章名时光标移动不受影响；OffsetSlider 是自定义 SwiftUI（非 NSSlider），不受影响
- 列表底部新增一行小字提示「拖拽可排序；选中后按 ↑/↓ 上下移动」，三语本地化（简/繁/英）

## [2.0.2] - 2026-09-05

### 改进
- **⌘Z / Ctrl+Z 统一时间线撤销**：撤销栈由「每枚章各自的几何历史」改为「全局按时间排序的动作时间线」，动作分两类——「添加章」与「几何改动（位置/大小/旋转/不透明度）」。⌘Z 现在撤销时间线上**最近一次**操作：
  - 撤销「添加章」= 移除该章（严格后进先出，添加顺序 A→B→C 时按 C→B→A 反序撤销）
  - 撤销「几何改动」= 还原该章改动前快照
  - 既保留原「把章的大小/位置回到上一状态」的能力，又新增「按相反顺序撤销添加」的能力，与访达/预览等主流软件一致
- **显式删除与撤销栈联动**：Delete 键、右栏「移除」按钮、⌘ 多选删除、移除全部，均会同步从撤销时间线清掉该章相关的「添加 / 几何」动作，避免撤销栈指向已不存在的章

### 行为变化（需留意）
- 几何撤销由「按当前选中章」改为「按时间最近一次」：常见场景（改完立刻 ⌘Z）完全一致；仅当「改了多枚、又去选中较早的一枚再 ⌘Z」时，会撤销最新那次改动而非选中那枚（标准行为）

## [2.0.1] - 2026-09-05

### 修复
- **另存为文件名后缀在中文界面下出错**：原实现把本地化键「文件名后缀」直接当作后缀写入文件名，简体/繁体界面下会生成「合同-文件名后缀.pdf」这类把说明文字当后缀的错误文件名；英文后缀 `seam-stamped`（骑缝）也与「同时支持正文章」的事实不符
- 改为按界面语言取专用后缀：简体「已盖章」、繁体「已蓋章」、英文 `stamped`；另存为默认文件名形如「检测报告-已盖章.pdf」「report-stamped.pdf」，保存位置仍默认为原文件所在文件夹
- **选中章后其他章点不中**：四角控制点原本渲染在选中章之后，在 ZStack 中夹在「选中章」与「更晚添加的章」之间，Z 序更低的章被它遮挡，只有更晚添加的章能点中；同时控制点用 `.position()` 定位，会把视图的布局 frame 撑满整个父容器，hit area 随之扩大到接近整页，连 hover（小手）与点击一并吞掉。现改为所有章渲染完后再统一绘制控制点（独立置于最上层），定位改用 `.offset()`，hit area 收回控制点自身大小
- **⌘ 点击多选此前不生效（检测方式错误）**：原实现在章的 `.onTapGesture` 闭包里读 `NSEvent.modifierFlags` 判定 ⌘，而该闭包是手势结束后**异步派发**的，此时全局修饰键状态常已重置，判定几乎恒为 false，导致 ⌘ 点击只是普通单选。现改为由 `ContentView` 的 `.flagsChanged` 全局监听器实时把 ⌘ 状态写入 `settings.commandKeyDown`，点击时直接读该实时值，判定可靠

### 改进
- **正文章选中态终于可见**：此前点击空白处新增章后会立刻把选中状态清空，而章本身又没有点击手势，导致选中框永远不会出现。现在新增章后自动进入选中态并显示控制框与右栏「章体大小」滑杆；再新增下一枚时选中自动转移到新章、上一枚自动取消；按 Esc 取消选中
- **选中视觉显著增强**：由 1.5px 橙色细虚线改为「白色衬底 + 系统强调色虚线」双层描边并叠加淡底色，在白纸 / 红色章面 / 深色背景上均清晰可辨
- **点击章本身即可选中**（拖动优先，纯点击才触发）；四角控制点统一为强调色并加大至 12–14px，更易抓取
- **右栏新增「已选中第 N 枚 / 共 M 枚」信息行 + 移除按钮**：选中预览中的章后，在「章体大小」上方显示当前是第几枚（共几枚），并提供移除按钮，删除该章后自动切到下一枚（保持选中），无章可切则自动取消选中
- **键盘 Delete 键删除选中章**：与右栏移除按钮同效；仅拦截裸按的 Delete（kVK_Delete = 51），带 ⌘/⌥ 修饰的组合键放行给系统，避免与「删除到行首」等编辑快捷键冲突；文本编辑器激活时（页码输入框等）完全不拦截
- **正文章支持多选（⌘ 点击累加）**：已有一枚或多枚章被选中时，按住 ⌘ 点击任意章会在选中集合里累加该章、其余已选中章的选中状态保持不变；⌘ 点击已选中的章则取消其选中（标准切换逻辑）。普通点击仍只选中被点章（清空其余）；按 Esc 取消全部选中。选中框为每枚选中章都显示，缩放手柄仅显示在主选中（最后点击/添加的章）上，与访达/预览一致
- **右栏「移除」按钮与 Delete 键支持多选**：选中多枚时「移除」按钮与 Delete 键一并删除所有选中章；「已选中第 N 枚 / 共 M 枚」改为「已选中 N 枚 / 共 M 枚」计数；尺寸滑杆仅在单选时显示（调整主选中章），多选时隐藏以避免歧义

## [2.0] - 2026-09-05

### 新增
- **新建图章对话框实时尺寸**：导入图片后，尺寸(cm) 输入框自动按图片真实物理尺寸填充（读取 DPI 元数据换算；无 DPI 信息按 300 DPI 估算），不再固定 4×4
- **选中章尺寸滑杆**：点击已添加的正文章选中后，右栏出现「章体大小」滑杆，实时等比调整选中章大小（1–20cm），上方实时显示「宽 × 高 cm」数值；松手固化到该章默认尺寸，⌘Z 可撤销
- **骑缝章多条错开**：支持同一 PDF 添加多条骑缝章实例，新增「上 ↔ 下」偏移滑杆（拖动实时移动最新一条），同页多章可上下错开互不覆盖
- **关闭确认**：点击「关闭」弹出「是否确认关闭？」确认框（取消/确定）
- **预览状态栏**：底部新增缩放百分比输入框（内置下拉预设 25/50/75/100/150/200%）与页码跳转输入框（显示「/ 总页数」）
- **双指捏合缩放**：触控板双指捏合实时缩放预览（10%–400%），与状态栏联动
- **保存/另存为/关闭按钮**：保存直接覆盖当前文件；另存为默认定位原文件夹、文件名默认「原文件名-骑缝章.pdf」

### 改进
- **章实例锁定印章**：每枚章（含骑缝章）添加时锁定所用印章图，切换印章库不影响已盖的章；支持一份 PDF 多章混盖（导出器升级为按放置项携带章图）
- **物理尺寸模型重构**：章实例直接存储宽/高物理厘米值，同一枚章在 A4/Letter 等不同纸张规格上保持相同实际大小；修复「新建图章设置宽高不生效」
- **深色模式**：全面适配系统深色外观（透明化预览棋盘格自动调暗）
- **三语言本地化**：界面跟随系统语言，支持 English / 简体中文 / 繁體中文
- **多语言 README**：新增英文版与繁体中文版，主 README 提供语言切换链接
- **代码清理**：移除无引用代码；保存/导出结果反馈显示于状态栏左侧
- **界面精简**：移除冗余滑杆与开关（启用开关、章体大小/位置偏移/不透明度/旋转滑杆），参数调整改由预览交互（点击落章、拖点缩放、滑杆）完成

### 修复
- 修复「全部页」只盖最后一页的问题（预览与导出统一范围逻辑）
- 修复 Esc 无法取消选中章、⌘Z 撤销失效（字段编辑器占据响应链导致按键拦截失效）
- 修复骑缝偏移滑杆标注上下颠倒、无法铺满行宽（自绘滑杆实现）
- 修复控制点与虚线框错位（热区参与布局导致偏移）

## [1.0] - 2026-09-04

### 首个公开版本
- SwiftUI + PDFKit/CoreGraphics 原生实现，零第三方依赖，纯本地处理
- 骑缝章：右/左/上/下缝位、页范围、按页删除与恢复
- 正文章：点击落章、拖动移动、右下角拖点缩放、右键按页/整章删除、多章实例
- 印章库：图片导入、白色转透明（容错可调、实时棋盘格预览）、长宽比锁定、物理尺寸设定（cm）
- 拼合校验视图：各页章条按页序拼回完整章对照检查
- 键盘操作：Esc 取消选中、⌘Z / Ctrl+Z 逐步撤销（每章独立撤销栈）
- 无头验证工具 SealTool（旋转页/混合尺寸页盖章回归测试）
